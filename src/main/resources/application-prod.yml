# Pegar as credenciais do keyvault
spring:
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/leap
    username: root
    password: admin
  jpa:
    open-in-view: false
    hibernate.ddl-auto: validate
    database: POSTGRESQL
    show-sql: false


---

jwt:
  key: "pegar_do_key_vault"

brapiFindTicket: "https://brapi.dev/api/quote"